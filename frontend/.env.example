# Production (Vercel) — URL de l'API Laravel
# VITE_API_BASE_URL=https://facturo-api.fly.dev
#
# Local : laisser commenté / vide. Vite proxyfie /api vers http://127.0.0.1:8000
# (lancer : cd backend && php artisan serve  +  cd frontend && npm run dev)
